({
    doInit : function(component, event, helper) {
        helper.handleInit(component, event);
    },

    doUpdate : function(component, event, helper) {
        helper.handleUpdate(component, event);
    }
})
